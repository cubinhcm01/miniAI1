# Repository Map

This map defines the known structure of `miniAI1`. Use this to target file reads without scanning the entire workspace.

```text
/c:/Users/kaybe/OneDrive/Máy tính/miniAI1/
├── .agents/                      # Agent Operating System (Context, Skills, Intelligence)
├── src/                          # Source Code Root
│   ├── MiniAI.Api/               # Presentation Layer (API endpoints, controllers)
│   ├── MiniAI.Application/       # Application Layer (Use cases, DTOs, interfaces)
│   ├── MiniAI.Domain/            # Domain Layer (Entities, core rules)
│   ├── MiniAI.Infrastructure/    # Infrastructure Layer (EF Core, services)
│   └── MiniAI.slnx               # Visual Studio Solution file
```

*Note: If the actual file structure differs from this map, the repository has drifted. You must enter Knowledge Maintenance Mode and propose an update to this map.*
